
  # Gym Split Calendar Layout

  This is a code bundle for Gym Split Calendar Layout. The original project is available at https://www.figma.com/design/HGNC3SEfXkfG5R1FubaZtX/Gym-Split-Calendar-Layout.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  